# 이 파일은 프로그램 실행에 필요한 외부 파이썬 패키지(라이브러리) 목록입니다.
# 터미널에서 'pip install -r requirements.txt' 명령어를 실행하면 아래 패키지들이 한 번에 설치됩니다.

# OpenAI API (ChatGPT) 사용을 위한 공식 패키지 (버전 1.0.0 이상 필요)
openai>=1.0.0

# .env 파일에서 환경 변수(API 키 등)를 안전하게 읽어오기 위한 패키지
python-dotenv>=1.0.0